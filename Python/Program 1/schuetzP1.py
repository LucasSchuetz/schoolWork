##  
#   CS161 Program One
#   Written by Lucas Schuetz
#   13 October 2014
#
#   This programs will take an input and output powers of this number
##

def main():

	numberStr = input("Hello, could you do me a favor and enter a number? ")
	num = int(numberStr)

	numPwr    = 0
	numResult = 1
	
	numPwr    = numPwr + 1
	numResult = numResult * num
	print("The number you chose is " + str(num) + ".")
	print(str(num) + " to the power of " + str(numPwr) + " is " + str(numResult))

	numPwr    = numPwr + 1
	numResult = numResult * num
	print(str(num) + " to the power of " + str(numPwr) + " is " + str(numResult))

	numPwr    = numPwr + 1
	numResult = numResult * num
	print(str(num) + " to the power of " + str(numPwr) + " is " + str(numResult))

	numPwr    = numPwr + 1
	numResult = numResult * num
	print(str(num) + " to the power of " + str(numPwr) + " is " + str(numResult))

	numPwr    = numPwr + 1
	numResult = numResult * num
	print(str(num) + " to the power of " + str(numPwr) + " is " + str(numResult))

	numPwr    = numPwr + 1
	numResult = numResult * num
	print(str(num) + " to the power of " + str(numPwr) + " is " + str(numResult))

	numPwr    = numPwr + 1
	numResult = numResult * num
	print(str(num) + " to the power of " + str(numPwr) + " is " + str(numResult))

	numPwr    = numPwr + 1
	numResult = numResult * num
	print(str(num) + " to the power of " + str(numPwr) + " is " + str(numResult))

	numPwr    = numPwr + 1
	numResult = numResult * num
	print(str(num) + " to the power of " + str(numPwr) + " is " + str(numResult))

	numPwr    = numPwr + 1
	numResult = numResult * num
	print(str(num) + " to the power of " + str(numPwr) + " is " + str(numResult))

	x = input("Test")

main()